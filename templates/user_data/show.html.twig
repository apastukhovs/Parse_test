{% extends 'base.html.twig' %}

{% block title %}UserData{% endblock %}

{% block body %}
    <h1>UserData</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_datum.id }}</td>
            </tr>
            <tr>
                <th>User_ip</th>
                <td>{{ user_datum.userIp }}</td>
            </tr>
            <tr>
                <th>User_browser</th>
                <td>{{ user_datum.userBrowser }}</td>
            </tr>
            <tr>
                <th>Time</th>
                <td>{{ user_datum.time ? user_datum.time|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_data_index') }}">back to list</a>

    <a href="{{ path('user_data_edit', {'id': user_datum.id}) }}">edit</a>

    {{ include('user_data/_delete_form.html.twig') }}
{% endblock %}
